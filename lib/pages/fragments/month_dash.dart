import 'package:flutter/material.dart';

class MonthDash extends StatelessWidget {
  const MonthDash({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: ListView(
        children: [],
      ),
    );
  }
}
